<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.msix.admin.board.dao.RBoardDAO">

<select id="replyList" parameterType="int" resultType="qnaReply">
	select r_no, q_no, r_title, r_content, 
	to_char(r_date,'YYYY-DD-MM') as r_date, r_cnt
	from r_board
	where q_no = #{q_no}
</select>

<insert id="replyInsert" parameterType="qnaReply">
	insert into r_board(r_no, q_no, r_title, r_content, r_cnt)
	values(r_no_seq.nextval, #{q_no}, #{r_title}, #{r_content}, 0)
</insert>

<update id="replyupdate" parameterType="qnaReply">
	update r_board
	set r_title = #{r_title}, r_content = #{r_content}, r_date = sysdate
	where r_no = #{r_no}
</update>

<delete id="replyDelete" parameterType="int">
	delete from r_board 
	where r_no = #{r_no}
</delete>

</mapper>