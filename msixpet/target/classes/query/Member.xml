<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.msixpet.client.member.dao.MemberDAO">
	
	<!-- 해당 아이디 중복 확인 -->
	<select id="idCheck" parameterType="member" resultType="int">
		select nvl((select 1 from member where m_id = #{m_id}), 0) as state from dual
	</select>
	
	<!-- 회원가입 -->
	<insert id="memberInsert" parameterType="member">
		INSERT INTO member(m_no, m_id, m_pwd, m_name, m_tel, m_email)
		VALUES (member_seq.nextval,#{m_id},#{m_pwd},#{m_name},#{m_tel},#{m_email})
	</insert>
	
	<select id="login" resultType="member">
		SELECT m_id, m_pwd FROM member WHERE m_id = #{m_id} AND m_pwd = #{m_pwd}
	</select>
</mapper>